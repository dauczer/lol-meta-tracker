name: 'lol_meta_tracker'
version: '1.0.0'
config-version: 2

profile: 'lol_meta_tracker'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  lol_meta_tracker:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
